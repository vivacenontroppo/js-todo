const taskList = document.getElementById('ul');
const taskInput = document.getElementById('in');
const newTaskButton = document.getElementById('addBtn');

export { taskList, taskInput, newTaskButton };